import React from 'react';
import PropTypes from 'prop-types';

const Single = (props) => {
  return <div className="single">Single Cloth Page</div>;
};

Single.propTypes = {
  item: PropTypes.object
};

export default Single;
