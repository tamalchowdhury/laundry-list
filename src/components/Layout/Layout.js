import React from 'react';
import PropTypes from 'prop-types';

const Layout = (props) => {
  return <div className="layout">This is my layout</div>;
};

Layout.propTypes = {};

export default Layout;
